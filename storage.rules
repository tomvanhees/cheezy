rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Cheese images — readable by all, writable by all (personal family app)
    match /cheeses/{cheeseId}/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
